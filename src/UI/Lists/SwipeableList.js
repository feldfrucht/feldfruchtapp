import React from 'react'
//import './SwipeableList.css'

const SwipeableList = ({children}) => {
    return( 
        <div className="swipeableList-list">{children}</div>
      );
};

export default SwipeableList